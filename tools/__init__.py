"""
GOLIATH Tools

Executable tool plugins that the AI can invoke (web search, file I/O,
code execution, etc.). Each tool exposes a standard interface so the
engine can discover and call it dynamically.
"""
